item = ["apple", "banana", "cherry", "orange", "mango"]

# use len() calculates the total count of items in the Array
# for example: len(item) return 5
# so it can return a value range to the for loop, so the loop will run 5 times
for index in range(len(item)):
    print(index, item[index])
